namespace SpectrumPC.Hardware
{
    public enum PulseTypeEnum
    {
        Data,
        Termination,
        Pilot,
        Sync1,
        Sync2,
        Stop,
        Pause
    }

    public enum JoystikButtons
    {
        Up = 0,
        Down = 1,
        Left = 2,
        Right = 3,
        Fire = 4,
    }
}
